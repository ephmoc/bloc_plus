# bloc_plus example

The example app demonstrates:

- `BlocConsumerWithEffects`
- policy composition with `and` / `whenRebuild`
- custom equality with `onChangeBy`
- `EffectListener.effectWhen`
- `MultiEffectListener`
- `RestartableTasksMixin`

Run:

```bash
cd example
flutter pub get
flutter run
```
